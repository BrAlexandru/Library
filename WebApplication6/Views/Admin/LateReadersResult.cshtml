@model IEnumerable<WebApplication6.Models.LateReaders>

@{
    ViewBag.Title = "LateReadersResult";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
<div class="display">
    <table>
        <tr>
            <th>Reader name</th>
            <th>Book name</th>
            <th>Expected date</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ReaderName</td>
                <td>@item.BookName</td>
                <td>@item.ExpectDate.ToString("dd MMM yyyy")</td>
            </tr>
        }
    </table>
</div>

